<h1>Good to see you again, <%= @user.first_name %></h1>

<p>Here is the last wine you drank:</p>

<%= @user.posts.last.title %>
<%= @user.posts.last.text %>
<%= image_tag @user.posts.last.image.url(:large) %>

<br>

<%= link_to '<button>Create New Post</button>'.html_safe, posts_new_path %>

<table>
  <tr>
      <th>Title</th>
      <th>Text</th>
      <th colspan='3'></th>
    </tr>

  <% @user.posts.each do |post| %>
    <tr>
      <td><%= post.title %></td>
      <td><%= post.text %></td>
      <td><%= link_to 'Show', post_path(post) %></td>
    </tr>

  <% end %>
</table>

<%= link_to 'Log Out', logout_path, method: :delete %>
